<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <%- include('../partials/head') %>
  <title>Home</title>
</head>

<body>
  <header>
    <%- include('../partials/header') %>
  </header>
  <main>
    <nav id="daily-image">
      <img src="../images/cover17.jpg" alt="" style="max-width: 100%;">
    </nav>
    <section class="home-body">

      <section class="article-preview">
        <% recentBooks.forEach((book) => { %>
      <div class="sub-article">
        <img src='<%= book.img_url %>' alt="">
          <h2><%= book.title %></h2>
          <a href='details/<%= book.id %>' >Show Detalis</a>
      </div>
      <% }); %>
      </section>

      <section class="card-article">
        <% if(mostBook) { %>
          <section id="top-article-section">
            <div id="image-container">
              <img src="<%= mostBook.img_url %>" alt="" srcset="">
            </div>
            <p><%= mostBook.description %></p>
            <h3><%= mostBook.author_first_name + " " + mostBook.author_last_name %></h3>
            <a href='details/<%= mostBook.id %>' >Show Detalis</a>
          </section>
          <% } else { %>
            <h1><%= "no clapped article" %></h1>
            <hr>
            <% }; %>
      </section>


    </section>
  </main>





</body>

</html>
